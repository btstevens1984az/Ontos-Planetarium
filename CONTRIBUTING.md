# Contributing

1. Never commit `.env`, databases, tokens, or host-specific paths.  
2. Keep demo data on RFC5737 / `example.invalid` ranges.  
3. Prefer small PRs with screenshots or a short GIF for UI changes.  
4. Run `pytest` under `backend/` and `npm run build` under `frontend/` before opening a PR.
